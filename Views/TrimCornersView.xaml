<UserControl x:Class="BimTasksV2.Views.TrimCornersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300"
             Background="#FAFAFA">

    <StackPanel Margin="16" VerticalAlignment="Center">
        <TextBlock Text="Wall Split Complete"
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="#1565C0" Margin="0,0,0,12"/>

        <TextBlock Text="OK any Revit warnings, then click the button below to fix corner connections."
                   TextWrapping="Wrap" Foreground="#555" FontSize="12"
                   Margin="0,0,0,16"/>

        <Button x:Name="TrimCornersButton"
                Content="Trim Corners"
                Height="36" FontSize="14" FontWeight="SemiBold"
                Background="#1565C0" Foreground="White"
                BorderThickness="0" Cursor="Hand"
                Click="TrimCornersButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4" Padding="16,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </StackPanel>
</UserControl>
