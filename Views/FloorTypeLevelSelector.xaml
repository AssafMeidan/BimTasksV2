<Window x:Class="BimTasksV2.Views.FloorTypeLevelSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Floor Type and Level"
        Width="420" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Floor Type:" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
        <ListBox Grid.Row="1"
                 x:Name="FloorTypeList"
                 DisplayMemberPath="Name"
                 Margin="0,0,0,16"
                 SelectionMode="Single"/>

        <TextBlock Grid.Row="2" Text="Level:" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
        <ListBox Grid.Row="3"
                 x:Name="LevelList"
                 DisplayMemberPath="Name"
                 Margin="0,0,0,16"
                 SelectionMode="Single"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Height="30" Margin="0,0,8,0"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Height="30"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
