<UserControl x:Class="BimTasksV2.Views.FilterTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BimTasksV2.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             ScrollViewer.HorizontalScrollBarVisibility="Auto" ClipToBounds="True">
    <UserControl.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>
    <Grid Margin="8" MinWidth="300" MinHeight="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding TopHeader}" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <RadioButton Content="And" GroupName="Mode"
                         IsChecked="{Binding IsAndMode}" Margin="0,0,16,0"/>
            <RadioButton Content="Or" GroupName="Mode"
                         IsChecked="{Binding IsAndMode, Converter={StaticResource InverseBooleanConverter}}"/>
        </StackPanel>

        <TreeView Grid.Row="2" ItemsSource="{Binding ElementTypes}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                 Checked="CheckBox_Toggle" Unchecked="CheckBox_Toggle"
                                 Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding DisplayName}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,8">
            <Button Content="OK" Width="80" Margin="0,0,8,0"
                    Command="{Binding OKCommand}"/>
            <Button Content="Filter Again" Width="80"
                    Command="{Binding ResetCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
